  // Define an enum for the game state
export enum GameState {
  Offline = 'offline',
  Waiting = 'waiting',
  Playing = 'playing',
  Winning = 'winning',
  Losing  = 'losing',
  Error   = 'error',
}
